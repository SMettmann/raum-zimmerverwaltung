{
  "name": "raum-zimmerverwaltung",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute raumwerk-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute raumwerk-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
